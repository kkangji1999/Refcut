# 의존성 — npm install 로 다시 받는다
node_modules/

# 빌드 결과물
dist/
out/

# 설치 파일에 넣을 도구 — npm run bin 이 다시 받는다 (17 MB 짜리라 올리지 않는다)
bin/

# 시험이 만들어내는 것들 (영상·뽑은 그림 — 돌리면 다시 생긴다)
test/_생성물/

# 로그
*.log
npm-debug.log*

# 작업용 메모 (저장소에 올리지 않는다)
요청*.txt

# OS / 편집기 찌꺼기
Thumbs.db
desktop.ini
.DS_Store
.vscode/
