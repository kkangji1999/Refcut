# Reftown — 설치하기

읽고 그대로 따라만 하시면 됩니다. **한 번만** 하면 끝입니다.

---

## 1단계 — Node.js 설치 (5분)

https://nodejs.org 에 들어가서 **LTS** 라고 적힌 큰 버튼을 누르세요.
받아진 파일을 실행하고 **[다음] 만 계속** 누르면 됩니다.

> Node.js 는 프로그램을 조립하는 도구입니다.
> 한 번 깔아두면 다음부터는 다시 안 해도 됩니다.

---

## 2단계 — 폴더 정리

받으신 파일을 이 모양으로 놓아주세요. 폴더 이름은 아무거나 괜찮습니다.

```
바탕화면\Reftown\
    main.js
    preload.js
    package.json
    app\               ← 이 폴더를 직접 만드세요
        index.html     ← 그 안에 넣으세요
```

**`app` 폴더를 만들어 그 안에 `index.html` 을 넣는 것**만 주의하시면 됩니다.

---

## 3단계 — 조립 (5분, 한 번만)

1. `Reftown` 폴더를 엽니다
2. 위쪽 **주소창을 클릭**하고 `cmd` 라고 친 뒤 Enter
3. 검은 창이 뜨면 아래를 붙여넣고 Enter

```
npm install
```

글자가 잔뜩 지나갑니다. **정상입니다.** 3~5분 기다리세요.

---

## 4단계 — 실행

같은 검은 창에:

```
npm start
```

창이 뜨면 **성공**입니다. 이제 15초 영상이 1~3초면 끝납니다.

---

## 5단계 — 바탕화면 아이콘 만들기 (선택)

매번 검은 창을 여는 게 번거로우면, 설치 파일을 만들면 됩니다.

```
npm run dist
```

`dist` 폴더 안에 **Reftown Setup.exe** 가 생깁니다.
그걸 실행하면 일반 프로그램처럼 설치되고 바탕화면 아이콘도 생깁니다.
팀원에게 그 exe 하나만 주면 됩니다. (받는 사람은 아무것도 설치할 필요 없습니다)

---

## 무엇이 달라지나

| | 지금까지 | Electron 판 |
|---|---|---|
| 15초 영상 분석 | 15~20초 | **1~3초** |
| 프레임 누락 | 되짚어 채워야 함 | 원리적으로 없음 |
| 저장 용량 | 브라우저 한도에 묶임 | 디스크 전체 |
| 저장 폴더 | 매번 권한 물어봄 | 자동 |
| 📁 위치 버튼 | 경로를 적어둬야 함 | **탐색기가 바로 열림** |
| 기록 | 브라우저 안, 못 꺼냄 | **JSON 파일** (복사하면 백업) |
| 창 크기 | 줄이면 찌그러짐 | 일정 크기 아래로 안 줄어듦 |

**컷을 찾아내는 방식은 하나도 바뀌지 않았습니다.**
ffmpeg 도 예전과 똑같이 320×180 으로 줄여서 넘겨주고,
계산하는 부분은 글자 하나 안 건드렸습니다. 그래서 결과가 같습니다.

---

## 기록이 저장되는 곳

```
%APPDATA%\Reftown\projects\
```

프로젝트마다 `.json` 파일 하나씩 생깁니다. **메모장으로 열립니다.**
이 폴더를 통째로 복사해두면 그게 백업입니다.

캡쳐한 이미지는 지정하신 저장 폴더에 따로 있습니다.
기본값은 `문서\Reftown` 이고, 저장 공간 화면에서 바꿀 수 있습니다.

---

## 막히면

검은 창에 빨간 글씨가 나오면 **그 내용을 통째로 복사해서** 알려주세요.
보고 바로 짚어드리겠습니다.

자주 있는 경우:
- `npm 은(는) 내부 또는 외부 명령... 이 아닙니다` → Node.js 설치 후 **컴퓨터를 다시 시작**
- 창은 뜨는데 화면이 하얗다 → `app` 폴더 안에 `index.html` 이 있는지 확인
