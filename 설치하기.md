# Reftown — 설치하기

읽고 그대로 따라만 하시면 됩니다. **한 번만** 하면 끝입니다.

---

## 1단계 — Node.js 설치 (5분)

https://nodejs.org 에 들어가서 **LTS** 라고 적힌 큰 버튼을 누르세요.
받아진 파일을 실행하고 **[다음] 만 계속** 누르면 됩니다.

> Node.js 는 프로그램을 조립하는 도구입니다.
> 한 번 깔아두면 다음부터는 다시 안 해도 됩니다.

---

## 2단계 — 폴더 정리

받으신 파일을 이 모양으로 놓아주세요. 폴더 이름은 아무거나 괜찮습니다.

```
바탕화면\Reftown\
    main.js
    preload.js
    package.json
    app\               ← 이 폴더를 직접 만드세요
        index.html     ← 그 안에 넣으세요
```

**`app` 폴더를 만들어 그 안에 `index.html` 을 넣는 것**만 주의하시면 됩니다.

---

## 3단계 — 조립 (5분, 한 번만)

1. `Reftown` 폴더를 엽니다
2. 위쪽 **주소창을 클릭**하고 `cmd` 라고 친 뒤 Enter
3. 검은 창이 뜨면 아래를 붙여넣고 Enter

```
npm install
```

글자가 잔뜩 지나갑니다. **정상입니다.** 3~5분 기다리세요.

---

## 4단계 — 실행

같은 검은 창에:

```
npm start
```

창이 뜨면 **성공**입니다. 이제 15초 영상이 1~3초면 끝납니다.

---

## 5단계 — 바탕화면 아이콘 만들기 (선택)

매번 검은 창을 여는 게 번거로우면, 설치 파일을 만들면 됩니다.

```
npm run dist
```

`dist` 폴더 안에 **Reftown Setup.exe** 가 생깁니다.
그걸 실행하면 일반 프로그램처럼 설치되고 바탕화면 아이콘도 생깁니다.
팀원에게 그 exe 하나만 주면 됩니다. (받는 사람은 아무것도 설치할 필요 없습니다)

---

## 설치가 도중에 멈출 때

업데이트를 누르거나 설치 파일을 실행했는데 이런 알람이 뜨는 경우가 있습니다.

```
Reftown cannot be closed. please close it manually and click retry
Failed to uninstall old application files. Please try running the installer again.:2
```

**[다시 시도] 를 눌러도 똑같습니다.** 그리고 그 뒤로는 프로그램이 아예
켜지지 않습니다 — 옛 버전은 지워졌는데 새 버전은 안 들어왔기 때문입니다.

### 왜 뜨나

프로그램이 꺼질 때, 영상을 다루려고 띄워둔 `ffmpeg` 를 그대로 두고 나갔습니다.
그런데 `ffmpeg` 는 설치 폴더 안에서 돌아갑니다. 윈도우는 **돌고 있는 프로그램의
파일을 지우지 못하므로**, 설치 프로그램이 옛 버전 폴더를 지우다 멈춘 것입니다.

백신이 깔린 컴퓨터에서 유난히 잘 났습니다. 실시간 검사가 그 파일들을 붙잡고
있는 시간이 겹쳐, 살아남은 `ffmpeg` 가 걸리는 창이 훨씬 넓어지기 때문입니다.

**v26.8.2512 부터 고쳤습니다.** 이제 꺼질 때 띄워둔 것을 하나도 남기지 않고
정리하고, 업데이트를 갈아끼우기 직전에도 한 번 더 정리합니다.

### 이미 깨진 컴퓨터를 되살리려면

1. 컴퓨터를 **재부팅**합니다 (Reftown 은 켜지 마세요)
2. **제어판 → 프로그램 추가/제거 → Reftown 제거** — 새로 까는 게 아니라 먼저 지웁니다
3. 설치 파일을 **우클릭 → 관리자 권한으로 실행**

설정과 기록은 `%APPDATA%Reftown` 에 따로 있어서 제거해도 남습니다.

그래도 같은 알람이면, **작업 관리자**(Ctrl+Shift+Esc) → **세부 정보** 탭에서
`Reftown.exe` · `ffmpeg.exe` · `yt-dlp.exe` · `qjs.exe` 를 모두 [작업 끝내기]
한 뒤 다시 설치해 주세요. 그것도 안 되면 `%LOCALAPPDATA%Programs` 안의
`Reftown` 폴더를 손으로 지우고 설치하면 됩니다.

> 확인하는 시험 : `npm run test:quit`
> 앱에게 오래 걸리는 일을 시켜놓고 꺼지는 길목을 지나게 한 뒤,
> 띄워둔 ffmpeg 가 하나도 안 남는지 봅니다.

---

## 백신이 뭐라고 할 때

동료 컴퓨터에서 **V3 · 알약 · 윈도우 디펜더** 가 경고를 띄우는 일이 있습니다.
**백신을 끄지 마세요.** 끌 필요가 없습니다.

### 왜 뜨나

이 프로그램은 영상 링크를 받아올 때 `yt-dlp` 라는 공개 도구를 씁니다.
파이썬을 exe 로 묶은 생김새라서 백신이 실제 위험이 아닌데도 자주 의심합니다.
그리고 설치 파일에 서명(코드 사이닝 인증서)이 없어서, 처음 설치할 때
윈도우가 **"알 수 없는 게시자"** 라는 파란 창을 띄웁니다.
그 창에서는 `추가 정보` → `실행` 을 누르면 됩니다.

### 이미 해둔 대비

| | 예전 | 지금 |
|---|---|---|
| yt-dlp | 처음 쓸 때 앱이 인터넷에서 받아옴 | **설치 파일에 처음부터 들어 있음** |
| 백신이 지웠을 때 | 이유 없이 "받지 못했습니다" | **백신 때문이라고 알려주고 폴더를 열어줌** |
| 지워진 뒤 | 계속 다시 받고 계속 지워짐 | **예외로 잡고 다시 누르면 저절로 되살아남** |

`앱이 인터넷에서 exe 를 받아 실행한다` 는 행동 자체를 없앴기 때문에,
백신이 의심할 거리가 크게 줄었습니다.

### 그래도 지워졌다면

앱이 안내창을 띄우고 **[그 폴더 열기]** 버튼을 줍니다.
백신 설정에서 그 폴더를 **[검사 제외]** (예외) 로 추가한 뒤 다시 누르면 됩니다.

```
%APPDATA%\Reftown\bin
```

회사 컴퓨터라 예외를 못 잡는다면, `yt-dlp.exe` 를 따로 받아 위 폴더에
직접 넣어도 됩니다. 파일이 그 자리에 있으면 프로그램은 새로 받지 않고 그대로 씁니다.

---

## 만드는 사람 몫 — 설치 파일에 넣을 도구

`npm run dist` / `npm run release` 가 **알아서 먼저** 받아옵니다.
따로 챙길 일은 없지만, 손으로 돌리려면:

```
npm run bin          없는 것만 받는다
npm run bin:force    있어도 새로 받는다 (최신으로 갈아끼울 때)
```

`bin\` 폴더에 `yt-dlp.exe` 와 `qjs.exe` 가 생기고, 그대로 설치 파일에 들어갑니다.
이 폴더는 저장소에 올리지 않습니다 (17 MB 짜리라 `.gitignore` 에 넣어뒀습니다).

> 오래된 `bin\` 을 그대로 두고 배포하면 몇 달 지난 yt-dlp 가 들어갑니다.
> 유튜브는 구조를 자주 바꾸므로, 내보내기 전에 `npm run bin:force` 를 한 번 돌려주세요.
> (설치한 뒤에는 앱이 하루 한 번 스스로 최신으로 맞춥니다)

---

## 무엇이 달라지나

| | 지금까지 | Electron 판 |
|---|---|---|
| 15초 영상 분석 | 15~20초 | **1~3초** |
| 프레임 누락 | 되짚어 채워야 함 | 원리적으로 없음 |
| 저장 용량 | 브라우저 한도에 묶임 | 디스크 전체 |
| 저장 폴더 | 매번 권한 물어봄 | 자동 |
| 📁 위치 버튼 | 경로를 적어둬야 함 | **탐색기가 바로 열림** |
| 기록 | 브라우저 안, 못 꺼냄 | **JSON 파일** (복사하면 백업) |
| 창 크기 | 줄이면 찌그러짐 | 일정 크기 아래로 안 줄어듦 |

**컷을 찾아내는 방식은 하나도 바뀌지 않았습니다.**
ffmpeg 도 예전과 똑같이 320×180 으로 줄여서 넘겨주고,
계산하는 부분은 글자 하나 안 건드렸습니다. 그래서 결과가 같습니다.

---

## 기록이 저장되는 곳

```
%APPDATA%\Reftown\projects\
```

프로젝트마다 `.json` 파일 하나씩 생깁니다. **메모장으로 열립니다.**
이 폴더를 통째로 복사해두면 그게 백업입니다.

캡쳐한 이미지는 지정하신 저장 폴더에 따로 있습니다.
기본값은 `문서\Reftown` 이고, 저장 공간 화면에서 바꿀 수 있습니다.

---

## 막히면

검은 창에 빨간 글씨가 나오면 **그 내용을 통째로 복사해서** 알려주세요.
보고 바로 짚어드리겠습니다.

자주 있는 경우:
- `npm 은(는) 내부 또는 외부 명령... 이 아닙니다` → Node.js 설치 후 **컴퓨터를 다시 시작**
- 창은 뜨는데 화면이 하얗다 → `app` 폴더 안에 `index.html` 이 있는지 확인
